pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Hola'
                sh '''
                  #!/bin/bash
                  ls
                  pwd
                  ls
                '''
            }
        }
    }
}
